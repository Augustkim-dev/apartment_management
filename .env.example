# ============================================
# 환경 변수 설정 예제
# 이 파일을 복사하여 .env.local로 만들고 실제 값을 입력하세요
# ============================================

# 환경 설정
NODE_ENV=production

# ============================================
# 데이터베이스 설정
# ============================================

# MySQL 연결 정보
MYSQL_HOST=your-mysql-server-ip-or-domain
MYSQL_PORT=3306
MYSQL_USER=your_database_user
MYSQL_PASSWORD=your_secure_database_password
MYSQL_DATABASE=coloco_apartment

# SSL 설정 (프로덕션 환경에서 권장)
MYSQL_SSL=true

# 연결 풀 설정
MYSQL_CONNECTION_LIMIT=5
MYSQL_QUEUE_LIMIT=0
MYSQL_CONNECT_TIMEOUT=60000

# ============================================
# 인증 설정
# ============================================

# NextAuth.js 설정
# 최소 32자 이상의 랜덤 문자열 사용
# 생성 방법: openssl rand -base64 32
NEXTAUTH_SECRET=your-production-nextauth-secret-minimum-32-characters

# 프로덕션 URL (Vercel 배포 후 설정)
NEXTAUTH_URL=https://your-domain.vercel.app

# JWT 설정
JWT_SECRET=your-production-jwt-secret-minimum-32-characters
JWT_EXPIRES_IN=24h

# 세션 암호화 키
SESSION_PASSWORD=your-production-session-password-minimum-32-characters

# ============================================
# 애플리케이션 설정
# ============================================

# 앱 URL (프로덕션)
NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app

# 앱 정보
NEXT_PUBLIC_APP_NAME=아르노빌리지 전기료 관리 시스템
NEXT_PUBLIC_APP_VERSION=1.0.0

# ============================================
# 파일 업로드 설정 (선택사항)
# ============================================

# 최대 파일 크기 (bytes)
MAX_FILE_SIZE=10485760  # 10MB

# 허용된 파일 확장자
ALLOWED_FILE_TYPES=pdf,xlsx,xls,csv

# ============================================
# 로깅 설정 (선택사항)
# ============================================

# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# ============================================
# Vercel 특정 설정 (자동 설정됨)
# ============================================

# VERCEL_ENV (production, preview, development)
# VERCEL_URL (자동 생성된 URL)
# VERCEL_REGION (배포 리전)

# ============================================
# 보안 주의사항
# ============================================

# 1. 이 파일을 절대 Git에 커밋하지 마세요
# 2. 프로덕션 환경에서는 강력한 비밀번호를 사용하세요
# 3. Vercel 대시보드에서 환경 변수를 직접 설정하세요
# 4. 정기적으로 비밀번호를 변경하세요